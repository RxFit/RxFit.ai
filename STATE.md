**Global Context:** We are building an on-brand /blog system on the RxFit.ai marketing site (Vite + React + Wouter + Express + Drizzle stack, dark SaaS aesthetic with teal #2DD4BF / coral #FB923C accents on slate #0F172A, Inter font, glassmorphism via glass-card utility in client/src/index.css). The blog drives organic traffic (SEO), gets cited by AI engines (AEO/GEO via TL;DR blocks, FAQPage JSON-LD, llms.txt, allowing GPTBot/PerplexityBot/ClaudeBot/Google-Extended in robots.txt), and converts readers to subscribers of app.rxfit.ai (CRO via inline tier-aware CTAs, sticky footer CTA, exit-intent modal, UTM attribution flowing into Stripe client_reference_id). Posts are MDX files in content/blog/*.mdx loaded at build time by @mdx-js/rollup, with frontmatter parsed by gray-matter. The MDX component library exposes brand-consistent primitives (<TLDR>, <KeyTakeaways>, <CTACard>, <FAQ>, <Stat>, <Callout>, <Comparison>) authors drop into posts.

Architectural rules the executor must honor: (1) Do NOT introduce a new backend — sitemap/robots/llms.txt are added as Express routes in the existing server/routes.ts. (2) Do NOT duplicate checkout logic — reuse the existing SignupModal and /api/stripe/checkout, /api/leads endpoints, refactored into a context provider so blog pages can trigger it. (3) Do NOT overwrite the default og:image / twitter:image tags in client/index.html — per-page meta layers on top via React-managed <head> updates. (4) Wouter is the router; do not introduce React Router. (5) The existing Stripe live price IDs hardcoded in LandingPage.tsx lines 272-276 must be referenced from a shared constants file after extraction so CTAs in blog posts use the same source of truth. (6) Every task below leaves the app in a working, type-checking, runnable state.

New dependencies (install once in TASK 1): @mdx-js/rollup, @mdx-js/react, gray-matter, remark-gfm, rehype-slug, rehype-autolink-headings, reading-time. No new backend deps. No DB schema changes — leads continue to use the existing leads table.


## TASKS

- [ ] TASK 1: Project Scaffolding (vite.config.ts, package.json, content/, shared/stripe-constants.ts) — Install MDX toolchain, configure Vite to load .mdx, create the content directory with an author README, and extract live Stripe price IDs into a shared module. * Pre-execution: Confirm vite.config.ts exists and inspect its current plugin array; check client/src/pages/LandingPage.tsx lines 272-276 for the live price ID literals; verify there is no existing content/ directory at the repo root. * Execution: 1. Install the new dependencies listed in Global Context. 2. Add @mdx-js/rollup to vite.config.ts plugins with remark-gfm, rehype-slug, rehype-autolink-headings configured; set providerImportSource: '@mdx-js/react'. 3. Create content/blog/ directory with a single .gitkeep and a content/blog/README.md describing frontmatter schema (title, slug, date, author, authorBio, authorPhoto, description, heroImage, tags[], pillar, targetKeyword, recommendedPlan), image conventions (/public/blog/<slug>/hero.jpg, ≤200KB), and the publish checklist (TL;DR, FAQ, ≥3 internal links, alt text, UTM on outbound). 4. Create shared/stripe-constants.ts exporting LIVE_PRICE_IDS = { kickstart, committed, transformation } with the same three IDs currently hardcoded in LandingPage.tsx. 5. Update LandingPage.tsx lines 272-276 to import from @shared/stripe-constants instead of inline literals. * Validation: Run npx tsc --noEmit and ensure 0 errors. Run npm run dev, confirm the landing page still renders, and confirm the pricing buttons still open the signup modal with no console errors.

- [ ] TASK 2: SignupModal Extraction + Context Provider (client/src/components/SignupModal.tsx, client/src/components/SignupModalProvider.tsx, client/src/App.tsx, client/src/pages/LandingPage.tsx) — Move the existing SignupModal from LandingPage.tsx into its own component file and wrap the app in a context provider so any page (blog posts, future pages) can open it with a preselected plan. * Pre-execution: Read client/src/pages/LandingPage.tsx lines 121-262 (current SignupModal definition) and lines 297-310 (current state hooks and modal usage). Note that the modal closes on backdrop click and uses framer-motion AnimatePresence. Note current livePriceIds map and openSignup(plan) helper. * Execution: 1. Move the SignupModal component verbatim to client/src/components/SignupModal.tsx, exporting it as default. 2. Create client/src/components/SignupModalProvider.tsx that wraps children, owns modalOpen + selectedPlan state, renders <SignupModal>, and exposes useSignupModal() returning { open: (plan: 'kickstart'|'committed'|'transformation') => void }. 3. Wrap the app tree inside <SignupModalProvider> in client/src/App.tsx (inside QueryClientProvider, outside Router). 4. In LandingPage.tsx, remove the inline modal state and SignupModal import, replace openSignup with const { open } = useSignupModal() so the existing pricing buttons call open(plan) instead. * Validation: Run npx tsc --noEmit (0 errors). Run npm run dev, click each of the 3 pricing tier buttons on the landing page, confirm the modal opens with the correct plan label in the heading. Confirm closing the modal works (backdrop + X button).

- [ ] TASK 3: Blog MDX Component Library (client/src/components/blog/MdxComponents.tsx) — Build all reusable post primitives in a single file so author content stays brand-consistent and AEO-optimized. * Pre-execution: Inspect client/src/index.css for existing utilities (glass-card, btn-primary, btn-secondary) and color tokens; confirm lucide-react, framer-motion, and @radix-ui/react-accordion (or check shadcn Accordion) are already available. * Execution: 1. Create client/src/components/blog/MdxComponents.tsx exporting a mdxComponents object plus named exports for: <TLDR> (teal-bordered glass card with "TL;DR" eyebrow, used at top of post for AI extraction), <KeyTakeaways> (orange-accented bulleted list with check icons), <CTACard plan="kickstart|committed|transformation"> (glass card with tier name, 1-line pitch, button that calls useSignupModal().open(plan) and fires cta_inline_click analytics event), <FAQ items={[{q,a}]}> (accordion that ALSO emits matching FAQPage JSON-LD via a <script type="application/ld+json"> tag — this is required for AEO), <Stat value="" label="" source=""> (large teal number + label + tiny outbound source link), <Callout type="tip|warning|info"> (left-bordered colored card), <Comparison columns={[]} rows={[]}> (responsive comparison table styled with glass-card). 2. Override default MDX elements: h1/h2/h3 with anchor links (use the rehype-slug IDs from TASK 1), a with target="_blank" rel="noopener" for outbound links + an internal-link variant that uses wouter's Link, img lazy-loaded with rounded corners, code/pre with dark syntax styling, blockquote with teal left border. 3. Export a single mdxComponents object combining all overrides for use by the post template's <MDXProvider>. * Validation: Run npx tsc --noEmit (0 errors). Briefly mount each component in isolation via a temporary throwaway route OR a Storybook-style sanity render inside the BlogPost preview in TASK 4 (do not commit a throwaway route — verify via TASK 4 integration).

- [ ] TASK 4: Blog Pages + Routing + Per-Page SEO (client/src/pages/BlogIndex.tsx, client/src/pages/BlogPost.tsx, client/src/lib/blogLoader.ts, client/src/lib/seo.tsx, client/src/App.tsx) — Build the index, post template, MDX loader, SEO/JSON-LD helper, and wire both routes. * Pre-execution: Inspect client/src/App.tsx to see how existing routes (/, /success) are registered with wouter. Inspect client/index.html for existing meta tags that must be preserved (especially default og:image, twitter:image, twitter:site). * Execution: 1. Create client/src/lib/blogLoader.ts that uses Vite's import.meta.glob('/content/blog/*.mdx', { eager: true }) to load all posts at build time, parse frontmatter, compute readingTime, sort by date desc, and expose getAllPosts() and getPostBySlug(slug). 2. Create client/src/lib/seo.tsx exporting a <Seo> component that updates document.title, manages <meta> tags (description, canonical, OG title/description/url/type=article, Twitter card) without removing existing default og:image/twitter:image tags, and injects Article + BreadcrumbList + Organization JSON-LD <script> tags into <head> (cleanup on unmount). 3. Build client/src/pages/BlogIndex.tsx: hero with 3 pillar filter chips (AI Coaching / Wearables / Accountability), responsive grid of post cards (hero image, tag chips, title, description, date, read time), on-brand glass styling, <Seo> for the index page. 4. Build client/src/pages/BlogPost.tsx: takes slug from wouter params, loads post via getPostBySlug, renders breadcrumb, H1, author byline with photo, hero image, sticky desktop TOC (built from H2s), MDX content wrapped in <MDXProvider components={mdxComponents}>, author bio card at the end, "Related posts" strip (3 from same pillar), <Seo> with frontmatter-driven meta. Show a 404-style fallback if the slug doesn't exist. 5. Register /blog → BlogIndex and /blog/:slug → BlogPost in client/src/App.tsx. * Validation: Run npx tsc --noEmit (0 errors). Run npm run dev. Even with zero posts present, /blog should render the empty-state hero. Create a single throwaway content/blog/_test.mdx with minimal frontmatter to confirm /blog/_test renders the post template, then DELETE that file before committing. Confirm no console errors and that the default <og:image> from client/index.html is NOT removed when navigating to blog pages.

- [ ] TASK 5: Crawlable Endpoints (server/routes.ts, client/public/llms.txt) — Add dynamic /sitemap.xml, static /robots.txt, and static /llms.txt so search engines and AI crawlers can discover and ingest the blog. * Pre-execution: Read server/routes.ts to confirm placement convention (Express handlers registered via registerRoutes); note that the file currently ends at the app.get("/api/diag/stripe-prices", ...) block before return httpServer. Confirm where Vite serves static files in dev vs production (server/static.ts and Vite public/). * Execution: 1. In server/routes.ts, add a GET /sitemap.xml route that imports the blog loader's compiled post list at runtime (use a server-safe equivalent: read content/blog/*.mdx filenames + frontmatter via fs + gray-matter at request time, cached for 5 minutes in-memory), emit valid XML sitemap with <url> entries for /, /blog, and every /blog/<slug> including <lastmod> from frontmatter date. 2. Add GET /robots.txt returning text: User-agent: * Allow /, Disallow /api/, /success; explicit Allow blocks for GPTBot, PerplexityBot, ClaudeBot, Google-Extended, CCBot; Sitemap: line pointing to the canonical sitemap URL (derive from request host so it works in both dev and production). 3. Create client/public/llms.txt (Vite serves client/public/ at the site root) containing a structured summary: "# RxFit.ai" intro paragraph, "## What we do" 2-line description, "## Key pages" linking to /, /blog, "## Pricing tiers" with the 3 tiers + prices, "## For AI assistants" telling them to cite blog posts as primary sources. Use the format proposed at llmstxt.org. * Validation: Run npx tsc --noEmit (0 errors). With npm run dev running, curl http://localhost:5000/sitemap.xml returns valid XML containing / and /blog. curl http://localhost:5000/robots.txt returns the expected directives. curl http://localhost:5000/llms.txt returns the markdown file. Confirm in browser that none of the three responses 404.

- [ ] TASK 6: CRO Surfaces + Analytics (client/src/components/blog/StickyFooterCta.tsx, client/src/components/blog/ExitIntentModal.tsx, client/src/lib/analytics.ts, client/src/lib/utm.ts, client/index.html, client/src/pages/BlogPost.tsx, server/routes.ts) — Add the sticky footer CTA, exit-intent modal, Plausible analytics, UTM utility, and propagate UTM through to Stripe client_reference_id. * Pre-execution: Confirm Plausible can be loaded via a single <script> in client/index.html (no env keys required for the free tier with a self-set domain). Note that exit-intent should be desktop-only (detect via matchMedia('(pointer: fine)')). Re-read server/routes.ts lines 142-183 (the /api/stripe/checkout route) to understand where to inject client_reference_id into the Stripe session params. * Execution: 1. Add the Plausible script tag to client/index.html <head> with data-domain="rxfit.ai" and defer (no-op in dev unless deployed, which is fine). 2. Create client/src/lib/analytics.ts with track(event, props) that calls window.plausible?.(event, { props }), safe to call when Plausible isn't loaded. 3. Create client/src/lib/utm.ts with getStoredUtm() (reads from sessionStorage, falling back to URLSearchParams on page load and persisting), appendUtm(url, slug), and getClientReferenceId() that returns a compact slug|source|medium|campaign string for Stripe attribution. 4. Build client/src/components/blog/StickyFooterCta.tsx: fixed bottom bar, dismissable (sessionStorage), hidden on /success, single "Try RxFit free for 7 days" button calling useSignupModal().open('kickstart') and track('cta_sticky_click', { slug }). 5. Build client/src/components/blog/ExitIntentModal.tsx: triggers on mouseleave to top of viewport, desktop only, once per session, submits to existing /api/leads with source: 'blog_exit_intent' and the current post slug, tracks cta_exit_modal_submit. 6. Wire both components into BlogPost.tsx; also fire pageview, scroll_50, scroll_90 via an IntersectionObserver or scroll listener with debounce. 7. In LandingPage.tsx's SignupModal mutation body (or the now-extracted SignupModal.tsx from TASK 2), append clientReferenceId: getClientReferenceId() to the POST body to /api/stripe/checkout. 8. In server/routes.ts checkout handler, accept clientReferenceId from the request body and pass it through to stripe.checkout.sessions.create({ ..., client_reference_id }) when present. * Validation: Run npx tsc --noEmit (0 errors). Run npm run dev, visit /blog/_test (re-create the throwaway post for this validation, delete after), confirm sticky footer is visible on desktop and mobile, confirm dismissing it persists in sessionStorage. Trigger exit-intent by moving the mouse to the top of the viewport — modal should appear once. Open the signup modal from any CTA and inspect the network request to /api/stripe/checkout: payload must include a non-empty clientReferenceId field.

- [ ] TASK 7: Seed Content — 3 Launch Posts (content/blog/ai-coach-vs-personal-trainer.mdx, content/blog/how-to-read-your-hrv.mdx, content/blog/accountability-gap-fitness-apps.mdx, client/public/blog/*) — Author 3 production-ready MDX posts covering the 3 pillars, each ~1,800-2,500 words and following the AEO format (TL;DR → KeyTakeaways → question-shaped H2s → FAQ → author bio). * Pre-execution: Confirm the MDX components from TASK 3 are all available and the BlogPost template from TASK 4 renders correctly. Check client/public/ exists and create client/public/blog/<slug>/ subdirectories for hero images. * Execution: 1. Author ai-coach-vs-personal-trainer.mdx (pillar: AI Coaching, target keyword: "AI fitness coach vs personal trainer", recommendedPlan: kickstart) — decision-stage comparison post using <TLDR>, <KeyTakeaways>, <Comparison> (cost/availability/personalization/data-driven rows), inline <CTACard plan="kickstart"> at ~60% scroll, <FAQ> with 5 Q&A pairs at the end. 2. Author how-to-read-your-hrv.mdx (pillar: Wearables, target keyword: "how to read HRV wearable", recommendedPlan: committed) — educational post with <Stat> blocks citing primary sources (Garmin/Apple/Whoop spec sheets, peer-reviewed HRV studies), <Callout type="tip"> blocks, <CTACard plan="committed">, <FAQ> with 6 Q&A pairs. 3. Author accountability-gap-fitness-apps.mdx (pillar: Accountability, target keyword: "why fitness apps fail accountability", recommendedPlan: transformation) — narrative problem-aware post citing dropout-rate studies, <KeyTakeaways>, <CTACard plan="transformation">, <FAQ> with 4 Q&A pairs. 4. For each post: add a 1200x630 hero image at client/public/blog/<slug>/hero.jpg (use placeholder branded images if no photos are available — solid slate background with teal/coral gradient + post title text), ensure ≥3 internal links between posts, all outbound links open in new tab. * Validation: Run npx tsc --noEmit (0 errors). Run npm run dev, visit /blog, confirm all 3 post cards render with hero images. Click into each post, confirm: TL;DR is visible at top, FAQ accordion expands, inline CTA opens signup modal with the correct preselected plan, sticky footer appears, <head> contains Article + BreadcrumbList + FAQPage JSON-LD (inspect via DevTools). View page source on each post and confirm <title> and <meta name="description"> come from frontmatter.

- [ ] TASK 8: Documentation + Final Pass (replit.md, content/blog/README.md) — Update project docs to reflect the new blog architecture and confirm the publishing workflow is clear. * Pre-execution: Re-read replit.md to see current "Project Architecture" and "Key Features" sections; re-read content/blog/README.md created in TASK 1 and refine now that the implementation is concrete. * Execution: 1. In replit.md, add a new section "## Blog & SEO" describing: blog lives at /blog, posts authored as MDX in content/blog/, MDX components in client/src/components/blog/MdxComponents.tsx, SEO meta + JSON-LD via client/src/lib/seo.tsx, sitemap/robots/llms.txt served by server/routes.ts, CRO surfaces (sticky footer, exit-intent, inline CTAs), Plausible analytics events tracked. 2. Add client/src/pages/BlogIndex.tsx, client/src/pages/BlogPost.tsx, client/src/components/SignupModal*.tsx, client/src/components/blog/*, client/src/lib/blogLoader.ts, client/src/lib/seo.tsx, client/src/lib/analytics.ts, client/src/lib/utm.ts, shared/stripe-constants.ts, client/public/llms.txt, content/blog/ to the "Project Architecture" file list with one-line descriptions. 3. Add Blog with SEO/AEO/CRO infrastructure to the "Key Features" bullet list. 4. Refine content/blog/README.md with a finalized publishing checklist now that all MDX components exist (list them with one-line descriptions and a code-fence example of each). * Validation: Run npx tsc --noEmit (0 errors). Run npm run dev one final time. Smoke test: (a) landing page pricing buttons still open signup modal with correct plan; (b) /blog lists 3 posts; (c) each post renders end-to-end with TL;DR, FAQ, CTAs, sticky footer; (d) curl /sitemap.xml, /robots.txt, /llms.txt all return 200 with expected content; (e) no console errors anywhere; (f) DevTools "Lighthouse" SEO score on a blog post is ≥95.